export enum PublicRouter {
    MAIN = '/',
    SIGNUP = '/signup',
    SIGNIN = '/signin',
}

export enum ProtectedRouter {
    MOVIES = '/movies',
    SAVED_MOVIES = '/saved-movies',
    PROFILE = '/profile',
}